---
- name: Execute 'show policy-map' on Cisco devices
  hosts: cisco
  gather_facts: no
  tasks:
    - name: Run 'show ip int brief'
      cisco.ios.ios_command:
        commands:
          - show ip interface brief
      register: output
    - name: Run 'policy map'
      cisco.ios.ios_command:
        commands:
          - show policy map
      register: show policy map
      
    - name: Display output
      debug:
        var: policy_map_output.stdout_lines
